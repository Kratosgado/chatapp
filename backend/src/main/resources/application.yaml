spring:
  application:
    name: chatapp

  datasource:
    url: jdbc:postgresql://localhost:5432/chatapp
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
